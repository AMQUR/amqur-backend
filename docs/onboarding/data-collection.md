# Data collection (before enablement)

Required: legal/display name, slug, website domains, phone, address, timezone, store hours (verified), logo URL, privacy/terms URLs, escalation emails, consent text, locale list.

Integrations: inventory feed URL+format, CRM webhook, service/parts systems (if any), Tekion IDs (if any).

Never paste production secrets into chat logs or tickets without a vault reference.
