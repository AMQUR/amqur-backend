# Security

- Tenant-scoped RBAC + widget role separation
- AES-256-GCM IntegrationSecret vault
- SSRF feed URL guard
- Webhook signature required for trusted processing
- No secrets in client; `.env.example` placeholders only
