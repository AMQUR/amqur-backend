# Deployment overview

Primary target: **GitHub + Railway** (API, worker, Postgres, Redis) + **static/CDN widget**.

Environments: `local` → `test` → `staging` → `production`.

See also: `railway.md`, `../operations/*`, root `.env.production.example`.
